package main

import (
	"fmt"
	"math"
	"strings"
)

func main(){
	//math.Floor(2.75)
	//strings.Title("head first go")
	//the math.Floor function, which rounds a floating-point number down to the nearest whole number
	fmt.Println(math.Floor(3.99))
	fmt.Println(strings.Title("head first go"))
}